server:
  port: 8080

kairos:
  firebase:
    enabled: ${KAIROS_FIREBASE_ENABLED:true}
    project-id: ${KAIROS_FIREBASE_PROJECT_ID}
    database-url: ${KAIROS_FIREBASE_DATABASE_URL}
    credentials-path: ${KAIROS_FIREBASE_CREDENTIALS_PATH:}
  cors:
    allowed-origins: ${KAIROS_CORS_ALLOWED_ORIGINS:http://localhost:5173,https://kairosmov.com.br}
